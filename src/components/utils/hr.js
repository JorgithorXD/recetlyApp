import { View } from "react-native"

export default function Separate ({backgroundColor}) {
    return (
        <View style = {{height: 2, marginBottom: 3, backgroundColor: backgroundColor}}></View>
    )
}

