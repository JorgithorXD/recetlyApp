import { View } from "react-native"

export default function Separate ({backgroundColor}) {
    return (
        <View style = {{height: 1, marginBottom: 3, backgroundColor: backgroundColor}}></View>
    )
}

